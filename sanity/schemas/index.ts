import blockContent from './blockContent';
import ebook from './ebook';

// Export this array and import it in sanity.config.ts as schema.types = schemas
export const schemas = [ebook, blockContent];

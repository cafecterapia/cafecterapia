// Placeholder CMS client. Swap with your actual CMS SDK (e.g., Sanity, Strapi) later.
export type CmsClient = unknown;

export function getCmsClient(): CmsClient {
  // Configure using env vars in .env.local when wiring a real CMS
  return {};
}
